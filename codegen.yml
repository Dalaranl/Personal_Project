schema: http://backend07.codebootcamp.co.kr/graphql # you can also point to a GraphQL endpoint!
generates:
    ./src/commons/types/generated/types.ts:
        plugins:
            - typescript
        config:
            typesPrefix: I
